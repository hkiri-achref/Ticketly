# Read automatically by Docker Compose (project dir = this folder when using
# `-f infra/docker-compose.yml`). Activates every DB profile so the F-00
# one-command bring-up still starts the whole platform; individual Spring Boot
# services activate only their own profile via
# spring.docker.compose.profiles.active.
COMPOSE_PROFILES=catalog,booking,payment,notification
